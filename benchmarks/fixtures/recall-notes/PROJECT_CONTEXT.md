# Project context
<!-- maintained by recall — stable facts only -->

## Stack
- Python 3.14, stdlib only, unittest via `python3 -m unittest discover -s tests -t .`

## Conventions
- Fixtures live in `benchmarks/fixtures/<skill>/`
