# Security contracts (placeholder)

Canonical security rules for bundled scripts and the `harden` skill's audit
checklists (OWASP Top 10:2025 baseline, ASVS 5.0.0 depth).

**Status:** intentionally minimal in v0.1. The safe-by-default script rules
that exist today live in [SECURITY.md](../../SECURITY.md). The full audit
framework lands with `harden` in Phase 4 — until then there is nothing here
to pretend with.
