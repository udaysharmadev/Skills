# Stack fixtures (planned)

Representative small repositories per PRD §12: TypeScript/web, Python,
Go or Rust, JVM backend, mobile/cross-platform. Used by workflow and
adversarial evals so skills prove decision quality across stacks, not just
tooling. Not started yet — added when the first workflow evals need them.
