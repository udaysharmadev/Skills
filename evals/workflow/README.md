# Workflow evals (planned)

Did the agent follow the skill's important phases — not just trigger it?

Populated per skill as they gain real usage: each skill contributes 2–3
phase-checkpoints (e.g. `hotseat`: independent round-1 verdicts before
cross-exposure; `masterplan`: real file inspection before slice
definition). Until a live runner exists, these are authored expectations,
not results.
