# Regression cases (planned)

Every behavior bug found in the field gets captured here as a case before
the fix merges: the triggering input, the wrong behavior, and the expected
behavior. Populated from real issues — empty until then.
